package Teste;

import java.util.ArrayList;
import java.util.Scanner;

/*Ideia
Apenas alguns testes. Nao criei classes para inimigos entao tive que instancia-los aqui
 */
public class main {
    
    public static void main(String args[]){
        
        /*testes
        Guerreiro guerreiro=new Guerreiro("Fafnir",1);
        Inimigo slime=new Inimigo("Slime",1,20,5,20);
        Inimigo orc=new Inimigo("Orc",1,15,10,10);
        ArrayList<Inimigo> inimigos=new ArrayList<Inimigo>();
        inimigos.add(slime);inimigos.add(orc);
        
        Scanner scanner=new Scanner(System.in);
        
        int saida=0;
        while(saida==0){//saida caso todos os inimigos estejam mortos
            System.out.print("Qual ataque usar?\n");
            guerreiro.atacar(inimigos, scanner.nextInt()-1);
            for(Inimigo temp:inimigos){
                if(temp.isDead())saida++;
            }
            if(saida!=inimigos.size())saida=0;
        }
        */
    }
}
